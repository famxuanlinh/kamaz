import React from 'react';
import ProductImage from '../ProductImage';

const ProductInfo = () => {
    return (
        <div>
            <ProductImage />
        </div>
    );
};

export default ProductInfo;
